%File extension in Prolog is "pl" like temp.pl
%closed-world hypothesis: the database defines what truth is.
%all truth is in the database.
%
%all procedures collectively form a database
%
%grouping of facts: procedures
%three individual facts
%isInteger procedure
isInteger(0).
isInteger(1).
isInteger(2).

%two individual facts
%isNmae procedures
isName(alice).
isNam(bob).

%query: questions we ask for the facts
%effectively program entry point

%types of data:
%-Numbers(0,1,2)
%-Atoms(alice,bob)
%  -start with a lowercase letter
%  -Effectively strings
%-Structure(later)

lessThan(1,2).
lessThan(1,2).
lessThan(2,3).

%starts with uppercase letter: variable
areEqual(X,X).
